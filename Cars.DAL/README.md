# Cars.DAL Library using EF Core

## Environment Setup
#### Install EF Tools for CLI
- dotnet tool install --global dotnet-ef --version 5.0.4

#### Create EF Migration
- dotnet ef migrations add "name"