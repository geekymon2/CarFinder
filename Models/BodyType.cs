namespace cars_api.Models
{
    public enum BodyType
    {
        SEDAN,
        HATCHBACK,
        SUV,
        UTE,
        WAGON,
        VAN,
        PEOPLEMOVER,
        CONVERTIBLE,
        COUPE,        
    }
}